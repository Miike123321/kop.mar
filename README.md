# kop.mar
kopiyka marketing app
🏗 Портал поставщиков
📋 Обзор проекта
Данное решение предназначено для автоматизации процесса подачи заявок поставщиками на участие в маркетинговых акциях. Система обеспечивает полный цикл: от создания черновика поставщиком до автоматизированной выгрузки утвержденных данных в PIM.

📂 Техническая структура и модули
1. Frontend Layer (UI/UX)
index.html — Основная рабочая область: форма подачи заявки. Реализована динамическая подгрузка SKU, расчет цен и выбор механик компенсации.
login.html — Модуль аутентификации. Разграничивает доступ между Поставщиками, Категорийными менеджерами (КМ) и Маркетингом.
proform.html — Специализированный интерфейс для генерации предзаполненных акционных форм.

2. Backend Layer (Business Logic & API)
server.js — Ядро на Node.js (Express). Обрабатывает маршруты, валидирует входящие данные и управляет состояниями объектов.
package.json — Конфиг среды (используются express для API и cors для кросс-доменных запросов).

3. Data Layer (Persistence)
db.json — Репозиторий заявок (applications). Каждая запись содержит метаданные товара, условия акции и финансовые показатели.
users.json — Реестр пользователей с RBAC-моделью (Role-Based Access Control).
actions.json — Справочник доступных промо-периодов и типов акций, синхронизированный с календарем маркетинга.

🔄 Жизненный цикл заявки и бизнес-процесс
Процесс построен на строгой статусной модели, исключающей хаос в данных:
Создание (Draft/New): Поставщик заполняет форму. Система проверяет артикулы и дедлайны через API.
Верификация (KM_Review): Категорийный менеджер получает уведомление. Он может подтвердить, отклонить или оставить комментарий.
Консолидация (Approved): Все подтвержденные заявки автоматически формируют "Сводную таблицу" (реестр участников).
Интеграция (PIM_Sync): Финальные данные экспортируются в PIM-систему через JSON/Queue для создания акционных документов.

🛠 Стек технологий
Runtime: Node.js
Web Framework: Express.js
Database: JSON Storage (File-based DB для MVP)
Architecture: Backend-first, RESTful API

🚀 Быстрый старт
Установка: Выполните npm install в директории backend/.
Запуск: Введите node server.js для старта API-сервера (порт по умолчанию 3000).
Доступ: Откройте frontend/login.html для входа в систему.
Контроль изменений: Все действия логируются в системе для обеспечения прозрачного аудита (Audit Trail). При масштабировании текущая JSON-модель данных легко мигрирует на PostgreSQL/MongoDB.